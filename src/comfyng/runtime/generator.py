from __future__ import annotations


def generate_workflow_image(*args, **kwargs):
    raise RuntimeError(
        "Legacy ComfyUI HTTP generation was removed. Use WorkflowDispatcher and the native FLUX runtime."
    )
